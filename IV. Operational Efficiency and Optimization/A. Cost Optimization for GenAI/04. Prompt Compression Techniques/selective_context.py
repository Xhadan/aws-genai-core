# Selective context parameters
compression_config = {
    'information_threshold': 0.5,  # Filter below this
    'preserve_structure': True,    # Keep document structure
    'min_chunk_size': 10          # Minimum tokens per chunk
}